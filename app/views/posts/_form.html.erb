<%= form_for :post do |p| %>
  <div class="row col s3">
    <div class="input-field">
      <i class="material-icons prefix">subject</i>
      <%= p.label :title %>
      <%= p.text_field :name %>
    </div>
    <div class="actions">
      <%= p.submit %>
    </div>
  </div>
<% end %>
